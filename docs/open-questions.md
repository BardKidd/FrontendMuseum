# 未裁決清單（2026-07-26）

> 登記檔（`docs/phase*-expected-results.md`）與程式、與彼此之間的不一致。
> **這份檔案不做裁決，只負責讓它們無處可躲。** 每一條都要嘛被裁決、要嘛被明文登記為「刻意保留」。
>
> `驗證` 欄的意思：
> - **實測** —— 本人跑過原始資料或程式碼直接對照過，結論確定
> - **未驗** —— 由查核產出，行號與敘述看起來成立，但沒有獨立覆核
>
> 規矩 2 是「登記完不准回頭改去迎合實測，要改就在檔尾修正紀錄追加」。
> 所以下面每一條的處置只有兩種：**追加一筆修正紀錄**，或**明文寫下為什麼保留原樣**。
> 靜靜地改掉登記值不算處置。

## 一、登記值與程式不一致

| # | 項目 | 登記 | 程式 | 驗證 |
|---|---|---|---|---|
| 1 | **#6 量測窗** | `phase2:138`「按下開始推送後 **10 秒**」 | `specimens/06-rerender-storm.ts:159` `STREAM_DURATION_MS = 5000` | **實測** |
| 2 | **#4 主指標名稱** | `phase2:100` `custom.droppedFrames` | `src/specimens.ts:270`／`tools/analyze-repro.mjs:53` 用 `droppedFramesPeak` | 未驗 |
| 3 | **#4 內容總高** | `phase2:75`「96,000px」（N=2000 推的） | N 已校準到 8000 ⇒ 應為 384,000px | 未驗 |

第 1 條額外有一層：`06-rerender-storm.ts:156-158` 宣稱「登記值已在該檔修正紀錄更正」，
但 `phase2` 全檔只有 `:138` 這一處提到量測窗，**沒有任何更正**。
註解宣稱的事實不存在 —— 依規矩 4 這是缺陷等級，不是整潔度問題。

第 2 條不是換個名字：`specimens.ts:268-269` 明寫「不用峰值的話跨輪比的是『你多久之後按重跑』」，
兩者**不是同一個量**。

## 二、登記值被實測否證，卻沒進任何作廢清單

| # | 項目 | 登記 | 實測 | 驗證 |
|---|---|---|---|---|
| 4 | **#1 `fixed-worker` 比值** | `phase1:107` `≥ 10×` | 3.0×（`phase1:402`）／本輪 1.9× | **實測**（本輪值） |
| 5 | **#1 `fixed-worker` INP** | `phase1:104` 4x `< 50ms` | 496~552ms（`phase1:402`）／本輪 448~608ms | **實測**（本輪值） |
| 6 | **#6 三個治療臂的 4x 區間** | `phase2:156-158` batch 20~120 / granular `< 30` / backpressure `< 15` | 上一輪三臂全部落空（`phase2:912-914`） | 未驗 |
| 7 | **#4 掉幀 38/46/43（18.6%）** | `phase2:438` | 舊 protocol（一拍一格）+ 舊 handler 的產物 | 未驗 |

第 4、5 條既沒作廢也沒被判「不符」，就這樣懸著。
而 `phase2:630` 目前還在拿第 7 條那個「`broken` 自身 18.6% 的離散度」當論據 ——
引用的是已作廢量測條件下的離散度。

## 三、判準本身沒有定論

| # | 項目 | 狀況 | 驗證 |
|---|---|---|---|
| 8 | **#4 比值判準** | 三個數字並存：`≥ 4×`（`phase2:110` 原始登記）、`≥ 3×`（`phase2:631` 括號內順帶寫的）、實測 2.2×。兩份作廢清單都沒處理前兩個 | 未驗 |
| 9 | **#6 比值判準** | `phase2:163` `≥ 6×`。舊煙測 40× 通過但參數已作廢；重設計後主指標 3.3%（且主指標飽和）。既沒作廢也沒重判 | 未驗 |
| 10 | **#5 治療臂登記值** | `phase2:256-257`（CLS `< 0.02`、比值 `> 10×`）在單一 `fixed` 臂拆成三臂（`:662-676`）之後**受詞消失**，未逐字作廢也未重新登記 | 未驗 |
| 11 | **#6 `renderRatio` 判別力** | `phase2:159` 的 `< 0.5` 在推送間隔 50ms → 25ms 之後判別力改變，未重新登記 | 未驗 |

## 四、登記的東西結構上量不到

| # | 項目 | 為什麼量不到 | 驗證 |
|---|---|---|---|
| 12 | **#2 的「INP presentation 段（捲動後點擊）」** | `phase2:210` 登記 1x 30~120 / 4x 100~400ms，但現行 protocol 是 `scroll`×10、**零點擊**（`src/specimens.ts:227-233`、`tools/reproducibility.mjs` 標 `inpBased: false`）。同一份文件 `phase2:49-51` 自己寫著「捲動與 wheel 不產生 `interactionId`」 | **實測**（分析輸出裡 #2 的兇手欄三臂皆 `—`） |
| 13 | **#5 的風險 R2** | `phase2:228-230` 要證明「沒設 `contain-intrinsic-size` 會製造 CLS」，但 `phase2:188` 已把它登記成必備、程式照做 ⇒ **沒有可執行的對照臂**。「本標本要順帶量 CLS」從未執行 | 未驗 |

第 12 條從登記那天起就不可能有數字。

## 五、「三輪兇手一致」這條判準的覆蓋範圍

`tools/analyze-repro.mjs` 已加「兇手 vs 登記值」比對（2026-07-26），六種狀態分開。
但它只在登記的 `culprit` 落在 INP 三段（`inputDelay` / `processing` / `presentation`）時才比對。

| 標本 | 登記的 culprit | 判準覆蓋到嗎 |
|---|---|---|
| #1 #3 | `presentation` / `processing` | ✅ 有效 |
| #2 | `lcp` | ❌ `not-an-inp-segment`，不報警 |
| #4 #6 | `loaf` | ❌ 同上 |
| #5 | `cls` | ❌ 同上 |

**六個標本裡有四個，登記的兇手從來沒有被任何判準驗證過。**
`phase2:1041-1042` 把這件事列為「仍未處理」—— 對 #2 / #4 / #5 / #6 仍然成立，只是原因變了：
不再是「判準不檢查登記值」，而是「判準的值域涵蓋不到這些指標」。**驗證：實測**（分析輸出）。

## 六、標的本身的身分不明

| # | 項目 | 狀況 | 驗證 |
|---|---|---|---|
| 14 | **#2 的 LCP 標的是誰** | `phase2:338-340` 登記「正式量測前值得確認那個 `p` 是誰」，後續三份修正紀錄都沒回頭處理。四份 JSON 的 `lcp.elementDescriptor` 全是裸 `p`（無 id 無 class）。**這是 LCP 標本，主指標指向的元素身分不明，兇手歸因那一節寫不下去** | **實測** |
| 15 | **#5 的 LCP 三臂標的不同** | `p#ls-caption` / `div#ls-prose` / `img#ls-figure` —— 跨臂比的不是同一個東西。#5 的主指標是 CLS 所以不影響判定，但 LCP 欄不得跨臂引用 | **實測** |

## 七、模板的已填範例（`docs/pathology-report-template.md`）

範例是唯一的品質標竿，被抄的機率最高。

| # | 項目 | 狀況 | 處置 |
|---|---|---|---|
| 16 | `layoutChecksum` 54168 | 三份 JSON × 兩 mode × 三輪共 18 筆全部是 **54888**。54168 出自 1x、四次點擊的校準探針 | ✅ **已修** |
| 17 | 「讀寫分離之後，強制版面**一幀都不再產生**」 | 0725 只有兩輪成立、0726 上午三輪都不成立（0.1/0.1/0.4ms）、本次掃描三輪成立。且同一份報告的 `:114` 與 `:156`（「三輪裡兩輪零幀」）互相矛盾 | 待裁決 |
| 18 | 「4x ÷ 1x = 4.0× ⇒ 節流確實生效」 | 跨 JSON 相除（1x 探針來自 phase1、4x 來自另一次 session，`phase1:455-456` 與 `phase2:944-945` 都明文禁止）。同一算式在 0726 上午給出 **7.12×** | 待裁決 |
| 19 | 治療梯度寫「forced median 0 ms／治療版該指標為零」 | 0726 上午是 0.1/0.1/0.4ms。模板 `:80` 自己給了正確寫法：「落進雜訊底線時不報比值」 | 待裁決 |
| 20 | 治療欄「0.1 / 0 / 0」 | 原始是 `0.1 / null / null`。模板前言明寫「『未量測』與『量到 0』是兩件事，**標本 #3 治療版的全部結論就建立在這個區別上**」—— 範例在示範這條規則的那個標本上寫了裸 0 | 待裁決 |

第 17~20 條的驗證狀態：**實測**（三份 JSON 的 `forcedMedian` / `forcedFn` / `loafPickedBy` 逐筆對照過）。

額外一條方法論陷阱，本次掃描實測到：`fixed-batched` 第一輪的 `forcedFn` 是
`batchedReadThenWrite` 但 `loafPickedBy` 是 **`specimenScriptDuration`** ——
那一幀是退回備援路徑選的，**`forcedFn` 不構成「有強制版面幀」的證據**。
這正是 `loafPickedBy` 存在的理由，引用 `forcedFn` 時必須同時看它。

## 八、面板顯示與實際程序不符

| # | 項目 | 狀況 | 驗證 |
|---|---|---|---|
| 21 | **`intervalMs: null` 印成「盡快連續（不要等畫面回應）」** | #5 的正確程序是「靜置三秒不要碰」、#6 是「按一次然後靜置五秒」。面板印的是相反的指示（`src/shell/App.tsx` 的操作程序那一行） | 未驗 |
| 22 | **`stream` vs `idle` 詞彙不同源** | #5 登記 `action: 'stream'`（`src/specimens.ts` 已自認是協定缺口），驅動器寫 `action: 'idle'` | 未驗 |
| 23 | **「已記錄 n / N」對 #6 恆為 1/1** | 按下開始推送的那一瞬間就顯示達標，而真正的量測窗才剛開始 | 未驗 |

第 21 條是**人手複驗會照著做錯事**的等級：文件告訴讀者的操作，和量測時實際做的不是同一件事。

## 九、文件落後

| # | 項目 | 驗證 |
|---|---|---|
| 24 | `phase2:547` 整節標題寫「量測工具自身的缺陷（**已知，未修**）」，底下五項全部已修 | 未驗 |
| 25 | `phase2:544` 把已修好的問題寫成待辦（「三處都還印著 35,000」，實際三處都已改） | 未驗 |
| 26 | `phase2:903`「四臂的 `wheelEvents` 一致（20）」—— `fixed-observer` 一個 wheel listener 都不掛，該欄必為 0 | 未驗 |
| 27 | `phase1:369` 一句「風險本身成真」蓋掉了同節一個**沒**成真的預測（預測「離散度全站最高、卡在 30% 線」，實測 broken 13.3%） | 未驗 |
| 28 | 行號漂移：`phase1:91` → `src/specimens.ts:87`（現行在 `:118`）／`phase1:227` → `App.tsx:437`（現行 `:543`）／`phase1:374,375` 引用 phase2 行號差一行 | 未驗 |

## 十、同一件事有兩組數字

| # | 項目 | 狀況 | 驗證 |
|---|---|---|---|
| 29 | **#3 的三輪 median** | `phase2:437` 表格是 745 / 716 / 678、9.2%；`phase2:446` 內文是「換成每輪 median 是 721 / 709 / 751ms、5.9%」。原始 JSON 的 `report` 區（`dispersion: 0.09248`）與 `records` 區（`forcedMedian` 744.5 / 716.35 / 678.25）**都支持前者**；721/709/751 在任何檔案裡找不到來源 | **實測** |

模板範例採了 9.2% 那組（正確）但沒說明為何不採另一組。

---

## 附：本輪已解決、不必再追的

- **B 類前導污染** —— 已修（deep-link + 丟棄式暖身），回歸測試 `tools/b-class-isolation.mjs`，
  診斷與作廢清單在 `docs/phase2-expected-results.md`「修正紀錄 · B 類前導污染」
- **輪轉守衛讓三輪只有兩種順序** —— 守衛已整個移除
- **`docs/HANDOFF.md` 的「不要拿本輪 #2 數字發表」** —— 禁令已解除，該條已改寫
- **`tools/reproducibility.mjs` 註解引用 1x 的 `32/32/40ms` 卻沒標條件** —— 已改成 4x 的
  `100 / 92 / 108ms`，並寫明舊數字是哪一版探針、為什麼差三倍
- **`layoutChecksum` 54168** —— 已改成 54888 並加註來源

---

## 處置狀態（2026-07-26 更新）

| 節 | 條目 | 處置 |
|---|---|---|
| 一 | 1 #6 量測窗、2 #4 主指標欄位名、3 #4 內容總高 | ✅ 追記現行有效值 → `phase2` 裁決紀錄 §一 |
| 二 | 4 #1 `fixed-worker ≥10×`、5 `INP < 50ms` | ✅ 補判為「數值落空」，並登記「混淆變因處理前不得單獨發表」 → §三 |
| 二 | 6 #6 三臂 4x 區間、7 #4 掉幀 38/46/43 | ⏳ 併入 §四「判準未定案」處理 |
| 三 | 8 #4 比值、9 #6 比值、10 #5 治療臂、11 #6 `renderRatio` | ⛔ **刻意不裁決** → `phase2` 裁決紀錄 §四。看過實測之後挑數字＝規矩 2 禁止的事。定案前這四支的比值判準視為不存在 |
| 四 | 12 #2 的 INP presentation 段、13 #5 的風險 R2 | ✅ 明文作廢（結構上量不到）→ §二 |
| 五 | 判準值域涵蓋不到四個標本 | ✅ 記錄，不修 → §五。已寫進第二篇文章 |
| 六 | 14 #2 的 LCP 標的是誰、15 #5 三臂標的不同 | ⏳ **未處理。** 14 是 LCP 標本主指標指向的元素身分不明，兇手歸因那節寫不下去，寫病理報告前必須解決 |
| 七 | 16 checksum | ✅ 已修（54888） |
| 七 | 17「一幀都不再產生」、18「4x÷1x=4.0×」、19「治療版該指標為零」、20 裸 0 | ✅ 四條全部改掉 → `docs/pathology-report-template.md`。18 是整段刪除，理由寫在原地 |
| 八 | 21 面板印錯操作程序、22 `stream`/`idle` 詞彙、23「已記錄 1/1」 | ⏳ **未處理。** 21 是「人手複驗會照著做錯事」的等級 |
| 九 | 24~28 文件落後、行號漂移 | ✅ 逐條更正 → §六、§七 |
| 十 | 29 #3 的兩組 median | ⏳ 未處理。原始 JSON 兩處都支持 745/716/678、9.2%；721/709/751 查無來源，可能出自 9 筆 `superseded` |

**還開著的四條**：六.14（#2 的 LCP 標的）、六.15、八.21~23（面板文案）、十.29。
其中 **六.14 與 八.21 會擋住病理報告**，動筆前要先解決。
